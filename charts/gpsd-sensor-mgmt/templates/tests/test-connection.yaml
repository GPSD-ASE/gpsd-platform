apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gpsd-sensor-mgmt.fullname" . }}-test-connection"
  labels:
    {{- include "gpsd-sensor-mgmt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gpsd-sensor-mgmt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
