apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpsd-sensor-mgmt.fullname" . }}
  labels:
    {{- include "gpsd-sensor-mgmt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gpsd-sensor-mgmt.selectorLabels" . | nindent 4 }}
