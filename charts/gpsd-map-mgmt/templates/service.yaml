apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpsd-map-mgmt.fullname" . }}
  labels:
    {{- include "gpsd-map-mgmt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gpsd-map-mgmt.selectorLabels" . | nindent 4 }}
