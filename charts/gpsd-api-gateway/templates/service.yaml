apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpsd-api-gateway.fullname" . }}
  labels:
    {{- include "gpsd-api-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gpsd-api-gateway.selectorLabels" . | nindent 4 }}
