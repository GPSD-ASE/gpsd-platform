apiVersion: v1
kind: Service
metadata:
  name: {{ include "gpsd-user-mgmt.fullname" . }}
  labels:
    {{- include "gpsd-user-mgmt.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gpsd-user-mgmt.selectorLabels" . | nindent 4 }}
